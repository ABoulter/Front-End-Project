export default function ProductPage() {
  /* const { art_id } = useParams(); */

  /*   const singleArt = arts.find((singleArt) => singleArt.arts.art_id === art_id);

  const { image, name } = singleArt; */

  return (
    <main>
      <h1>oi</h1>
    </main>
    /*     <main id="products">
      {arts.map((category) => (
        <section key={category.category_id}>
          <h2>{category.category}</h2>
        </section>
      ))}
    </main> */
  );
}
